from django.conf.urls import patterns, url

urlpatterns = [
    # Examples:
    # url(r'^$', 'test_assignment.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),

    # url(r'^admin/', include(admin.site.urls)),
]
